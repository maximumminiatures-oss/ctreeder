[pytest]
# By default, run unit/integration tests only.
# E2E tests (Playwright) require a live server + browser — run them explicitly:
#   pytest tests/e2e/ -v
testpaths = tests
addopts = --ignore=tests/e2e
